# lambda functions  is the function in for of expression

# lambda the variable on which we want an operation : the operation that we want to perform
square = lambda x :x*x

print(square(5))

add = lambda a, b, c: a+b+c
print(add(2,2,2))
